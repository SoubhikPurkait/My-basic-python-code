# A function is a group of task which is perform the specific operation as the needs of the programmer 
# when the code is bigger is size then we can perform the task using the function by using function in small code 



def avg():  #function defintin
    a=int(input("Enter the number: "))
    b=int(input("Enter the number: "))
    c=int(input("Enter the number: "))

    average=(a+b+c)/3
    print(average)

avg()  #Function call
avg()
avg()